% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mypaintr.R
\name{mypaint_device}
\alias{mypaint_device}
\title{Open a libmypaint-backed graphics device}
\usage{
mypaint_device(
  filename = NULL,
  file = NULL,
  width = 7,
  height = 7,
  res = 144,
  pointsize = 12,
  bg = "white",
  brush = "ink",
  brush_settings = NULL,
  stroke_style = NULL,
  fill_style = NULL,
  fill_brush = NULL,
  fill_settings = NULL,
  hand = NULL,
  stroke_hand = NULL,
  fill_hand = NULL,
  auto_solid_bg = TRUE
)
}
\arguments{
\item{filename}{Output PNG filename. If it contains \verb{\\\%d}, pages are
numbered.}

\item{file}{Deprecated compatibility alias for \code{filename}.}

\item{width, height}{Device size in inches.}

\item{res}{Resolution in pixels per inch.}

\item{pointsize}{Base pointsize.}

\item{bg}{Background colour.}

\item{brush}{Stroke brush preset, installed mypaint brush name,
\code{.myb} file path, JSON brush string, named settings, or \code{NULL} for solid
strokes.}

\item{brush_settings}{Named settings overriding \code{brush}.}

\item{stroke_style}{Legacy override for whether stroke drawing uses the brush
backend or solid Cairo rendering. When \code{NULL}, this is inferred from
whether \code{brush} is \code{NULL}.}

\item{fill_style}{Legacy override for whether fill drawing uses the brush
backend or solid Cairo rendering. When \code{NULL}, this is inferred from
whether \code{fill_brush} is \code{NULL}.}

\item{fill_brush}{Optional fill brush spec. Defaults to \code{brush} when not
supplied. Use explicit \code{NULL} for solid fills.}

\item{fill_settings}{Named settings overriding \code{fill_brush}.}

\item{hand}{Optional hand-drawn geometry spec applied to both stroke and
fill primitives by default.}

\item{stroke_hand}{Optional hand-drawn geometry spec for strokes.}

\item{fill_hand}{Optional hand-drawn geometry spec for fills.}

\item{auto_solid_bg}{Draw large fills that match the device background using
normal Cairo rendering even when \code{fill_style = "brush"}.}
}
\value{
Opens a graphics device and returns \code{NULL} invisibly.
}
\description{
Open a libmypaint-backed graphics device
}
\examples{
out <- tempfile("mypaint-basic-", fileext = "-\%d.png")
mypaint_device(out, width = 4, height = 3, bg = "ivory")
plot(
  1:10,
  col = "steelblue",
  pch = 16,
  cex = 1.4,
  main = "Ink Lines"
)
lines(1:10, col = "firebrick", lwd = 3)
rect(2, 3, 5, 7, border = "black", col = rgb(0, 0.6, 0.3, 0.25))
text(6, 8, "hello", col = "black")
dev.off()
unlink(Sys.glob(sub("\%d", "*", out, fixed = TRUE)))

out <- tempfile("mypaint-brush-", fileext = "-\%d.png")
mypaint_device(
  out,
  width = 4,
  height = 3,
  brush = "chalk",
  brush_settings = c(tracking_noise = 0.12),
  fill_style = "brush",
  fill_brush = "pencil",
  fill_settings = c(radius_by_random = 0.08)
)
plot.new()
plot.window(xlim = c(0, 10), ylim = c(0, 10))
polygon(
  c(2, 5, 8, 6, 3),
  c(2, 7, 6, 3, 1.5),
  border = "black",
  col = rgb(0.2, 0.7, 0.5, 0.6)
)
lines(c(1, 9), c(1, 9), col = "firebrick", lwd = 4)
title("Brush Fill")
box()
dev.off()
unlink(Sys.glob(sub("\%d", "*", out, fixed = TRUE)))

out <- tempfile("mypaint-mixed-", fileext = "-\%d.png")
mypaint_device(out, width = 4, height = 3, brush = NULL)
plot(1:5, 1:5, type = "n", main = "Mixed Styles")
set_brush("pencil", type = "stroke")
lines(1:5, c(1, 3, 2, 5, 4), lwd = 3)
dev.off()
unlink(Sys.glob(sub("\%d", "*", out, fixed = TRUE)))
}

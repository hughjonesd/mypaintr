% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot.R
\name{element_mypaint_line}
\alias{element_mypaint_line}
\title{Theme line element with scoped mypaint rendering}
\usage{
element_mypaint_line(
  brush = NULL,
  hand = NULL,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  lineend = NULL,
  color = NULL,
  linejoin = NULL,
  arrow = FALSE,
  arrow.fill = NULL,
  inherit.blank = FALSE,
  size = NULL,
  ...
)
}
\arguments{
\item{brush}{Stroke brush specification created with \code{\link[=tweak_brush]{tweak_brush()}}, an
installed mypaint brush name, \code{.myb} file path, JSON brush string, or
\code{NULL} for solid strokes.}

\item{hand}{Optional hand-drawn geometry created with \code{\link[=hand]{hand()}}.}

\item{colour, color, linewidth, linetype, lineend, linejoin, arrow, arrow.fill, inherit.blank, size, ...}{Passed through to \code{\link[ggplot2:element]{ggplot2::element_line()}}.}
}
\value{
A ggplot theme element.
}
\description{
Uses the current \code{mypaint} device for drawing, but temporarily overrides the
stroke settings while the theme line is drawn. This is useful for keeping
axes, ticks, or panel grid lines solid while data layers use rough or brush
rendering.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::theme(axis.line = element_mypaint_line())
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hands.R
\name{speed_flat}
\alias{speed_flat}
\alias{speed_human}
\title{Speed profiles for hand-drawn strokes}
\usage{
speed_flat(value = 1)

speed_human(
  value = 1,
  taper = 0.65,
  start = 0.55,
  end = 0.65,
  peak = 1.4,
  peak_at = 0.45,
  turn_slowdown = 0.45,
  min = 0.05
)
}
\arguments{
\item{value}{Base speed multiplier. \code{1} preserves the default
distance-based timing heuristic, values greater than \code{1} draw faster, and
values below \code{1} draw slower.}

\item{taper}{How strongly speed changes over the stroke. \code{0} keeps the speed
flat; \code{1} applies the full profile shape.}

\item{start, end}{Relative speed at the start and end of a human-style stroke
when \code{taper = 1}.}

\item{peak}{Relative peak speed reached during the stroke when \code{taper = 1}.}

\item{peak_at}{Position of peak speed along the stroke, in the range \code{0} to
\code{1}.}

\item{turn_slowdown}{How strongly speed is reduced at sharp turns.}

\item{min}{Minimum speed multiplier returned by the profile.}
}
\value{
A speed-profile function for the \code{speed} argument of \code{\link[=hand]{hand()}} and
\code{\link[=human_hand]{human_hand()}}. Custom functions can also be supplied directly; they must
accept \code{t}, normalized stroke progress in the range \code{0} to \code{1}, and \code{turn}
in the range \code{0} to \code{1}. Speed profiles return positive speed
multipliers.
}
\description{
Speed profiles for hand-drawn strokes
}
\examples{
plot.new()
plot.window(c(0, 10), c(0, 10))
draw_rough_lines(c(1, 9), c(8, 8), lwd = 5,
                 hand = hand(speed = speed_flat(0.5)))
draw_rough_lines(c(1, 9), c(5, 5), lwd = 5,
                 hand = hand(speed = speed_human()))
}
\concept{speed profiles}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hands.R
\name{hand}
\alias{hand}
\alias{human_hand}
\title{Hand-drawn geometry settings}
\usage{
hand(
  seed = NULL,
  bow = 0,
  wobble = 0,
  multi_stroke = 1L,
  width_jitter = 0,
  endpoint_jitter = 0,
  pressure = pressure_flat(),
  speed = 1,
  xtilt = 0,
  ytilt = 0,
  barrel_rotation = 0
)

human_hand(
  seed = NULL,
  bow = 0.004,
  wobble = 0.004,
  multi_stroke = 1L,
  width_jitter = 0.08,
  endpoint_jitter = 0,
  pressure = pressure_human(),
  speed = 1,
  xtilt = 0,
  ytilt = 0,
  barrel_rotation = 0
)
}
\arguments{
\item{seed}{Optional random seed used for repeatable geometry.}

\item{bow}{Typical bowing of long strokes as a proportion of segment length.}

\item{wobble}{Low-frequency path wobble as a proportion of segment length.}

\item{multi_stroke}{Number of overdrawn strokes to use.}

\item{width_jitter}{Relative variation in line width between overdrawn
strokes.}

\item{endpoint_jitter}{Relative endpoint jitter as a proportion of segment
length.}

\item{pressure}{Pressure profile function, typically created with
\code{\link[=pressure_flat]{pressure_flat()}}, \code{\link[=pressure_smooth]{pressure_smooth()}}, or \code{\link[=pressure_human]{pressure_human()}}. A single
number is treated as \code{pressure_flat(pressure)}.}

\item{speed}{Synthetic brush speed multiplier for \code{\link[=mypaint_device]{mypaint_device()}} brush
rendering. \code{1} preserves the default distance-based timing heuristic;
values greater than \code{1} draw faster, and values below \code{1} draw slower.}

\item{xtilt, ytilt}{Stylus tilt inputs passed to libmypaint, in its normalized
\code{-1} to \code{1} range.}

\item{barrel_rotation}{Stylus barrel rotation, in degrees.}
}
\value{
An object describing how rough geometry should be generated.

An object describing how rough geometry should be generated.
}
\description{
\code{human_hand()} is the same as \code{\link[=hand]{hand()}}, but starts from rougher defaults with
bow, wobble, width jitter, and a human-style pressure profile enabled.
}
\details{
\code{hand()} defaults to plain, base-R-like geometry with no bowing, wobble, or
jitter and flat pressure. \code{\link[=human_hand]{human_hand()}} has different, more human-like
defaults, including \code{\link[=pressure_human]{pressure_human()}}.

As of now, pressure profiles only apply to open lines, not shape outlines. On
base R devices, they are simulated and affect line width.

The \code{speed}, \code{xtilt}, \code{ytilt}, and \code{barrel_rotation} arguments affect only
brush rendering on \code{\link[=mypaint_device]{mypaint_device()}}. They are ignored by standard graphics
devices.
}
\examples{
plot.new()
plot.window(c(0, 10), c(0, 10))
draw_rough_lines(c(0, 10), c(8, 8), lwd = 4, hand = hand())
draw_rough_lines(c(0, 10), c(6, 6), lwd = 4, hand = human_hand())
draw_rough_lines(c(0, 10), c(4, 4), lwd = 4,
                 hand = human_hand(seed = 1,
                   bow = 0.02, wobble = 0.01))
draw_rough_lines(c(0, 10), c(2, 2), lwd = 4,
                 hand = human_hand(seed = 1,
                   pressure = pressure_smooth(0.7, taper = 0.5)))
draw_rough_lines(c(0, 10), c(1, 1), lwd = 4,
                 hand = human_hand(seed = 1,
                   pressure = pressure_human()))
}

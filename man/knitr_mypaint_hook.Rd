% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitr-hook.R
\name{knitr_mypaint_hook}
\alias{knitr_mypaint_hook}
\title{Create a knitr chunk hook for live mypaint rendering}
\usage{
knitr_mypaint_hook(...)
}
\arguments{
\item{...}{Default arguments passed through to \code{\link[=mypaint_device]{mypaint_device()}} when the
hook opens a device. Chunk-specific overrides can be supplied in the chunk
option \code{mypaint.args} as a named list. Because these arguments are applied
when the device opens, use this hook or \code{mypaint.args} to set chunk
defaults; \code{\link[=set_brush]{set_brush()}} and \code{\link[=set_hand]{set_hand()}} still work within the chunk after
the device is open.}
}
\value{
A function suitable for \code{knitr::knit_hooks$set()}.
}
\description{
The returned hook opens \code{\link[=mypaint_device]{mypaint_device()}} before chunk evaluation and
injects the generated PNG files afterward. This avoids knitr's normal plot
replay path, which does not preserve device-local style changes such as
\code{\link[=set_hand]{set_hand()}} and \code{\link[=set_brush]{set_brush()}}.
}
\details{
Register it with \code{knitr::knit_hooks$set(mypaint = knitr_mypaint_hook(...))}
and then enable it for chunks with \code{mypaint = TRUE}. Chunks should also set
\code{fig.keep = "none"} and \code{fig.ext = "png"}. If a chunk explicitly sets
\verb{dev=}, the hook is skipped and knitr's normal device handling is used.
}
\examples{
if (requireNamespace("knitr", quietly = TRUE)) {
  hook <- knitr_mypaint_hook(brush = "deevad/2B_pencil")
  print(is.function(hook))
}
}

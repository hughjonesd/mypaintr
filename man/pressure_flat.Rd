% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hands.R
\name{pressure_flat}
\alias{pressure_flat}
\alias{pressure_smooth}
\alias{pressure_human}
\title{Pressure profiles for hand-drawn strokes}
\usage{
pressure_flat(value = 1)

pressure_smooth(value = 1, taper = 1, turn_taper = 0.35)

pressure_human(
  value = 1,
  taper = 0.6,
  start = 0.35,
  end = 0.55,
  peak = 0.45,
  turn_taper = 0.35
)
}
\arguments{
\item{value}{Maximum pressure supplied to the brush, in the range \code{0} to
\code{1}.}

\item{taper}{How strongly pressure changes over the stroke. \code{0} keeps the
pressure flat; \code{1} applies the full profile shape.}

\item{turn_taper}{How strongly pressure is reduced at sharp turns.}

\item{start, end}{Relative pressure at the start and end of a human-style
stroke when \code{taper = 1}.}

\item{peak}{Position of peak pressure along the stroke, in the range \code{0} to
\code{1}.}
}
\value{
A pressure-profile function for the \code{pressure} argument of \code{\link[=hand]{hand()}}
and \code{\link[=human_hand]{human_hand()}}. Custom functions can also be supplied directly; they
must accept \code{t}, normalized stroke progress in the range \code{0} to \code{1}, and
\code{turn} in the range \code{0} to \code{1}. \code{turn} describes local path curvature:
\code{0} is straight, larger values are sharper corners, and values near \code{1}
are near reversals.
}
\description{
Pressure profiles for hand-drawn strokes
}
\examples{
plot.new()
plot.window(c(0, 10), c(0, 10))
draw_rough_lines(c(1, 9), c(8, 8), lwd = 5,
                 hand = hand(pressure = pressure_flat(0.5)))
draw_rough_lines(c(1, 9), c(5, 5), lwd = 5,
                 hand = hand(pressure = pressure_smooth()))
draw_rough_lines(c(1, 9), c(2, 2), lwd = 5,
                 hand = hand(pressure = pressure_human()))
}
\concept{pressure profiles}

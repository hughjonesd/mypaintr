% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot.R
\name{element_mypaint_rect}
\alias{element_mypaint_rect}
\title{Theme rectangle element with scoped mypaint rendering}
\usage{
element_mypaint_rect(
  brush = NULL,
  brush_settings = NULL,
  fill_brush = NULL,
  fill_settings = NULL,
  hand = NULL,
  stroke_hand = hand,
  fill_hand = hand,
  auto_solid_bg = NULL,
  fill = NULL,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  color = NULL,
  linejoin = NULL,
  inherit.blank = FALSE,
  size = NULL,
  ...
)
}
\arguments{
\item{brush}{Stroke brush preset, installed brush name, JSON brush string,
named settings, or \code{NULL} for solid borders.}

\item{brush_settings}{Named settings overriding \code{brush}.}

\item{fill_brush}{Fill brush preset, installed brush name, JSON brush string,
named settings, or \code{NULL} for solid fills.}

\item{fill_settings}{Named settings overriding \code{fill_brush}.}

\item{hand}{Optional hand-drawn geometry applied to both stroke and fill by
default.}

\item{stroke_hand}{Optional hand-drawn geometry for the border.}

\item{fill_hand}{Optional hand-drawn geometry for the fill.}

\item{auto_solid_bg}{Optional override for background-like fills.}

\item{fill, colour, color, linewidth, linetype, linejoin, inherit.blank, size, ...}{Passed through to \code{\link[ggplot2:element]{ggplot2::element_rect()}}.}
}
\value{
A ggplot theme element.
}
\description{
Uses the current \code{mypaint} device for drawing, but temporarily overrides the
stroke and fill settings while the rectangle is drawn. This is useful for
panel backgrounds, panel borders, and legend keys in ggplot2 themes.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::theme(panel.background = element_mypaint_rect())
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mypaintr.R
\name{geom_mypaint_col}
\alias{geom_mypaint_col}
\title{Draw rough, brush-rendered columns in ggplot2}
\usage{
geom_mypaint_col(
  mapping = NULL,
  data = NULL,
  position = "stack",
  ...,
  just = 0.5,
  lineend = "butt",
  linejoin = "mitre",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  fill_pattern = NULL,
  brush = NULL,
  brush_settings = NULL,
  fill_brush = NULL,
  fill_settings = NULL,
  hand = NULL,
  stroke_hand = hand,
  fill_hand = hand,
  auto_solid_bg = NULL
)
}
\arguments{
\item{mapping, data, position, just, lineend, linejoin, na.rm, show.legend, inherit.aes}{As for \code{\link[ggplot2:geom_bar]{ggplot2::geom_col()}}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{fill_pattern}{Optional fill pattern created with \code{\link[=hatch]{hatch()}},
\code{\link[=crosshatch]{crosshatch()}}, \code{\link[=zigzag]{zigzag()}}, or \code{\link[=jumble]{jumble()}}. When omitted, bars use a simple
hatch fill by default.}

\item{brush, brush_settings}{Stroke brush spec and overrides.}

\item{fill_brush, fill_settings}{Fill-hatch brush spec and overrides.}

\item{hand}{Optional hand-drawn geometry applied to both outline and hatch by
default.}

\item{stroke_hand}{Optional hand-drawn geometry for the outline.}

\item{fill_hand}{Optional hand-drawn geometry for the hatch strokes.}

\item{auto_solid_bg}{Reserved for future parity with device-level style
controls.}
}
\value{
A ggplot layer.
}
\description{
This geom owns both the bar outline and the hatch fill, so the shading lines
follow the same rough outline rather than the underlying true rectangle.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::ggplot(mtcars, ggplot2::aes(factor(cyl))) +
    geom_mypaint_bar(fill_pattern = hatch())
}
}
